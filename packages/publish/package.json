{
  "name": "@pgegypt/publish",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src || true",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --reporter=verbose",
    "build": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pgegypt/db": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.9.2"
  }
}
